# Create an API key in DeepSeek Platform; never commit a real key to Git.
# Without it the demo uses a visibly labelled local fallback.
DEEPSEEK_API_KEY=your_api_key_here
DEEPSEEK_MODEL=deepseek-chat
DATA_PATH=./data/atoms-demo.db
PORT=8080
