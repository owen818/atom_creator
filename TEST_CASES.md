# Atoms Forge 完整测试用例

## 1. 测试前置条件

1. 后端已启动：`mvn spring-boot:run`。
2. 前端已启动：`cd frontend && npm run dev`。
3. 访问 `http://localhost:5173`，使用新的测试账号。
4. `.env` 配置有效的 `DEEPSEEK_API_KEY`；模型失败保护用例除外。
5. 首次启动后确认 SQLite 自动创建 `users`、`projects`、`generations`、`agent_runs` 四张表。

## 2. 基础账户与项目

| ID | 优先级 | 场景 | 步骤 | 预期结果 |
|---|---|---|---|---|
| AUTH-01 | P0 | 注册 | 输入合法名称、邮箱和 6 位以上密码，点击注册 | 成功进入工作台；SQLite 写入用户记录 |
| AUTH-02 | P0 | 重复邮箱 | 使用 AUTH-01 邮箱再次注册 | 显示“该邮箱已注册”；不创建重复用户 |
| AUTH-03 | P1 | 输入校验 | 分别提交空邮箱、无效邮箱、短密码 | 显示校验错误；不创建用户 |
| AUTH-04 | P0 | 登录 | 退出后使用已注册账号登录 | 成功进入工作台，历史项目可见 |
| PROJ-01 | P0 | 新建项目 | 点击“新建应用”，填写需求，选择“创建应用” | 进入可编辑计划阶段；创建项目状态为 `PLAN_READY` |
| PROJ-02 | P1 | 项目隔离 | 使用第二账号登录 | 第二账号不可查看第一账号项目 |
| PROJ-03 | P1 | 删除项目 | 删除一个测试项目并刷新页面 | 项目及全部版本被删除，其他项目不受影响 |

## 3. Agent 计划、批准、取消

创建应用提示词：

```text
创建一个独立咖啡馆预约网站，包含菜单分类筛选、商品卡片、营业时间、门店介绍、预约表单和预约成功提示。页面采用深色高级风格，所有交互均可使用。
```

| ID | 优先级 | 场景 | 步骤 | 预期结果 |
|---|---|---|---|---|
| AGENT-01 | P0 | 生成计划 | 输入创建提示词，点击“生成计划” | 显示 Agent Run、可编辑计划、`AWAITING_APPROVAL` 阶段；此时无新生成版本 |
| AGENT-02 | P0 | 编辑计划 | 在计划中增加“预约成功后显示提示”，点击“批准并执行” | 保存编辑后的计划，随后开始执行 |
| AGENT-03 | P0 | 可观察执行 | 批准后观察 Agent Run | 日志按顺序出现：读取上下文、调用模型、回归检查、保存版本；最终为 `COMPLETED` 或明确 `FAILED` |
| AGENT-04 | P0 | 人工批准门槛 | 仅生成计划，不点击批准 | 不调用代码生成；项目没有新增版本 |
| AGENT-05 | P1 | 计划取消 | 在 `PLAN_READY` 时点击“取消计划” | 任务为 `CANCELLED`，不生成版本 |
| AGENT-06 | P1 | 执行取消 | 批准后、完成前点击“取消执行” | 状态为 `CANCELLED`；模型请求若已发出，完成后不得保存新版本 |
| AGENT-07 | P1 | 失败可见性 | 临时配置无效 Key 后批准执行 | 状态、日志或回退提供明确结果；页面不静默失败 |

## 4. 生成、增量修改与版本

| ID | 优先级 | 场景 | 步骤 | 预期结果 |
|---|---|---|---|---|
| GEN-01 | P0 | 首次生成 | 批准 AGENT-01 的计划 | 出现 `v1`、状态 `READY`、右侧 iframe 显示生成应用 |
| GEN-02 | P0 | 菜单交互 | 在预览中点击“咖啡”“甜点”等筛选项 | 菜单卡片按分类变化，主工作台不刷新 |
| GEN-03 | P0 | 预约交互 | 打开预约弹窗，填写合法信息并提交 | 显示预约成功提示；弹窗按页面设计关闭或保留 |
| GEN-04 | P0 | 增量修改 | 选择已有项目及“增量修改”，输入下方提示，生成计划并批准 | 创建 `v2`；版本 v1 保留；页面主色变绿，菜单和预约功能仍存在 |
| GEN-05 | P1 | 版本回看 | 在版本历史点击 v1、v2 | 预览切换到对应 HTML；不改变最新版本 |
| GEN-06 | P1 | 模型失败保留旧版 | 对已有项目临时移除 Key，选择“增量修改”并执行 | 新版本标识 `Local fallback`，HTML 保留上一版本，不丢功能 |

GEN-04 提示词：

```text
只将页面主色调调整为墨绿色和奶油白色，将主要操作按钮改为绿色。不要修改或删除菜单筛选、商品卡片、营业时间、预约表单、预约成功提示及现有交互功能。
```

## 5. 真实 Bug 修复与回归

当前咖啡馆页面存在可复现问题：手机号字段只判断非空，输入 `abc` 也可能提交成功；日期字段也可能允许选择过去日期。

| ID | 优先级 | 场景 | 步骤 | 预期结果 |
|---|---|---|---|---|
| BUG-01 | P0 | 复现手机号 Bug | 打开预约表单，填写姓名，手机号填 `abc`，选择日期和时段后提交 | 修复前可错误显示预约成功，或至少未给出手机号格式提示；记录截图 |
| BUG-02 | P0 | 准备修复计划 | 选择“修复 Bug”，使用下方提示，生成计划 | 计划明确包括手机号格式、过去日期校验、保留现有功能 |
| BUG-03 | P0 | 批准并修复 | 编辑/批准 BUG-02 计划，等待完成 | 新版本生成，Agent 日志包含回归阶段 |
| BUG-04 | P0 | 验证非法手机号 | 在新版本输入 `abc` | 显示格式错误；不关闭弹窗、不清空表单、不显示预约成功 |
| BUG-05 | P0 | 验证合法手机号 | 输入 `13800138000`，填写完整信息 | 允许提交并显示预约成功提示 |
| BUG-06 | P0 | 验证日期 | 尝试选择今天之前日期 | 日期不可选择，或提交时收到明确提示 |
| BUG-07 | P0 | 回归验证 | 在修复版本中测试菜单筛选、打开/关闭预约弹窗、合法预约 | 原菜单筛选、预约表单、成功提示仍然可用；结构回归结果为 `PASS` 或有明确 `WARN` 说明 |

BUG-02 提示词：

```text
Bug：预约表单的手机号字段只校验是否为空，输入 abc 等非法内容仍可预约成功；同时禁止用户预约今天之前的日期。

请修复：手机号必须是中国大陆 11 位手机号，以 1 开头，第二位为 3 到 9。手机号不合法时显示明确提示，不关闭弹窗也不清空表单；日期不能早于今天。修复手机号 placeholder 为“请输入手机号”。

不要修改菜单筛选、商品卡片、营业时间、预约弹窗、预约成功提示和现有视觉风格。
```

## 6. 代码查看与交付

| ID | 优先级 | 场景 | 步骤 | 预期结果 |
|---|---|---|---|---|
| EXP-01 | P0 | 查看代码 | 切换到任意版本，点击“查看代码” | 显示该版本完整 HTML 源码 |
| EXP-02 | P1 | 复制代码 | 在代码面板点击“复制” | 浏览器剪贴板包含完整 HTML；可粘贴到文本编辑器 |
| EXP-03 | P0 | ZIP 导出 | 选中 v1 后点击“ZIP 导出”，解压；再选 v2 重复 | ZIP 含 `index.html`、`README.md`；版本号对应所选版本；本地打开 HTML 可运行 |

## 7. 持久化与部署冒烟测试

| ID | 优先级 | 场景 | 步骤 | 预期结果 |
|---|---|---|---|---|
| OPS-01 | P0 | SQLite 持久化 | 创建至少两个版本，停止后端，重新启动并登录 | 项目、版本、代码和预览仍可访问 |
| OPS-02 | P1 | 容器入口 | 构建 Docker 镜像后访问 `/` | 显示 Vue 页面，不能出现 Spring Whitelabel 404 |
| OPS-03 | P1 | Render 端口 | 在 Render 设置 Secret 后部署 | 服务自动读取 `PORT`；根路径可访问；DeepSeek Key 不出现在页面源码或日志中 |
| OPS-04 | P2 | 免费实例限制 | 重启 Render 免费实例后检查 | 页面仍可打开；如没有持久卷，记录 SQLite 数据可能丢失的已知限制 |

## 8. 通过标准与演示路径

最小验收通过条件：AUTH-01、AGENT-01 至 AGENT-04、GEN-01、GEN-04、BUG-02 至 BUG-07、EXP-01、EXP-03、OPS-01 全部通过。

推荐演示顺序：

```text
注册登录
→ 创建应用并审阅计划
→ 批准执行、观察阶段日志
→ 增量修改颜色且保留功能
→ 复现手机号 Bug
→ 修复 Bug 并展示回归结果
→ 回看历史版本
→ 查看代码与导出 ZIP
```

> 当前回归检查是透明的结构检查（按钮、表单、脚本数量变化），并配合上述手工交互回归。它不替代 Playwright/Selenium 等完整浏览器 E2E 自动化测试。
